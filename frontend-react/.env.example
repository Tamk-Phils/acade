# Vercel / Frontend Deployment Environment Variables
# If backend is deployed separately, set VITE_API_URL to the backend URL without trailing slash:
# VITE_API_URL=https://acadformat-api.onrender.com
# If left empty, API calls default to relative path /api (proxied via same domain or rewrites).
VITE_API_URL=

